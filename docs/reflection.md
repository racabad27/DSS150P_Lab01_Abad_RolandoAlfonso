1. Which source would be easiest to integrate into a future pipeline, and why?
   - It would be the customers.csv. This is mainly due to the organized structured form that it takes form of because this will allow for easier analysis and reading when being used in the near future. Another thing to note is the overall cleanliness of the dataset, gathering a near perfect subject when it comes to overall data quality. It may not be the perfect one, but comparing it to something like a nested-structure is definitely more practical to choose over a semi-structured dataset. Keeping it simple is the best way to go about it, since the process itself can double in complication depending on how the dataset looks like.
2. Which source presents the greatest schema or data-quality risk, and what evidence supports your answer?
   - I would say the orders.json. This is because it contains nested structures or semi-structured fields that caused me real errors like 'TypeError: unhashable type: dict'. Furthermore, This kind of dataset must be taken into consideration when performing things like parsing because it can easily end up erroring in that department as well.This could infer that the next holder might encounter the same problem once going through the same steps that were taken within making this whole activity. This is why it is important to choose a simple but sure dataset, rather than a potential dataset that is unclear on what path it may follow or result to in the end.

3. What could go wrong if a pipeline is built before the source schema and contract are understood?
   - If the pipeline is built without any source schema and contract, then it will simply result into a lot of incoming errors and problems that the system needs in order to communicate with one another. All of the components within this system need to be functional because they each serve their own purpose, and one cannot function with the other. Rather, one cannot function PROPERLY without the prescence of a compatible or housing component. It will ultimately lead to the downstreaming to be comprimised, inflation of datasets due to counting duplicates, and it can also mismatch datatypes from specific fields.
4. How do Git, virtual environments, containers, and documentation improve reproducibility for a data-engineering team?
   - GIT simply allows us to communicate and to show the progress that we have made. It allows for reproducibility and recoverability in case something goes bad within the system. Virtual environments are there allows for us to fullfill the role of installing the dependencies with respect to whats in the requirements.txt. Containers are there storage within docker itself. Lastly, documentation is for people to see how you did it and why you got that result.